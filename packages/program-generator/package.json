{
  "name": "@fitness/program-generator",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic training-program generator: intake answers in, a full/lower or upper/lower split with concrete exercises out. Pure functions, no I/O.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^3.2.4"
  }
}
