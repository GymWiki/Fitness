{
  "name": "@fitness/progression-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic, explainable progression rules for strength and cardio training. Pure functions, no I/O, no LLM calls.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^3.2.4"
  }
}
