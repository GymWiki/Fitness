{
  "name": "@fitness/nutrition-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic calorie/macro targets and a protein-adequacy signal, derived from body metrics and training goal. Pure functions, no I/O, no LLM calls.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^3.2.4"
  }
}
